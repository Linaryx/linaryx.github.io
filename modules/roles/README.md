Roles module (фронт):
- Компоненты/композиции для ролей/аватаров.
- Работа с кэшем `public/api-cache/roles`.
- Live запросы к внешнему публичному API как fallback.

Содержимое:
- `composables/` — useRoles, кэширование.
- `lib/` — клиенты 2807 API / ivr.
